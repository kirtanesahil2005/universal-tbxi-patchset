; ---------------------------------------------------------------------------

loc_FFC388B0:                           ; DATA XREF: ROM:ToolboxTraps↓o
                movea.l ($BC8).w,a0
                movea.l ($2B6).w,a1
                lea     $124(a1),a1
                move.l  ($668).w,-(sp)
                rts
; ---------------------------------------------------------------------------
